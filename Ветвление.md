---
date: 
tags: 
authors: GB

Abstract:  
---
# [Если, if](zotero://open-pdf/library/items/7WNSNKIW?page=21&annotation=LADBUQX9)

---

В Python доступны все 6 операций сравнения:

![[_attachments/Установка и настройка Python/image-21-x56-y184.png]]

Простейшая проверка условия:
```python
pwd = "text&"  
res = input("Input password: ")  
if res == pwd:  
	print("Доступ разрешён")  
	print("Но будьте осторожны")  
print("Работа завершена")"
```

***❗❗❗ PEP-8 рекомендует использовать в качестве отступа 4 пробела. 
        IDE при верной настройке заменяют нажатие  клавиши TAB на клавиатуре на ввод 4 пробелов. ***

# [Иначе, else](zotero://open-pdf/library/items/7WNSNKIW?page=22&annotation=MB24H9EA)
---

Для выполнения кода в случае ложности логического выражения используется  зарезервированное слово `else` c обязательным двоеточием после него.

```python
pwd = "text"
res = input("Input password: ")  
if res == pwd:  
	print("Доступ разрешён")  
	print("Но будьте осторожны")  
else:
	print("Доступ запрещён")
print("Работа завершена")
```

Слово else относится к тому if, с которым находится на одном уровне.

```python
pwd = "text"
res = input("Input password: ")  
if res == pwd:  
	print("Доступ разрешён")  
	my_math = int(input("2 + 2 = "))  
	if 2 + 2 == my_math:  
		print("Вы в нормальном мире")  
	else:  
		print("Но будьте осторожны")  
else:  
	print("Доступ запрещён")  
print("Работа завершена")
```

# [Еще если, elif](zotero://open-pdf/library/items/7WNSNKIW?page=23&annotation=MGW6VP5M)
---

Для проверки нескольких выражений используется elfi - сокращение от else if.

```python
color = input("Твой любимый цвет: ")  
if color == "красный":  
	print("Любитель яркого")  
elif color == "зелёный":  
	print("Ты не охотник?")  
elif color == "синий":  
	print("Ха, классика!")  
else:  
	print("Тебя не понять")
```

Проверка работает до первого совпадения. После него дальнейший код  пропускается. А если совпадений нет, срабатывает код после else.

# [Выбор из вариантов, match и case](zotero://open-pdf/library/items/7WNSNKIW?page=23&annotation=TU2YGFSF)

После `match` указываем  переменную для сравнения. Далее идёт блок из множества `case` с вариантами  сравнения.

***❗❗❗ Если у вас стоит Python версии 3.9 и ниже, код не будет работать. ***

```python
color = input("Твой любимый цвет: ")  
match color:  
	case "красный" | "оранжевый":  
		print("Любитель яркого")  
	case "зелёный":  
		print("Ты не охотник?")  
	case "синий" | "голубой":  
		print("Ха, классика!")  
	case _:  
		print("Тебя не понять")
```

Вместо слова `else` в данной конструкции используется сочетание `case _`

# [Логические конструкции, or, and, not](zotero://open-pdf/library/items/7WNSNKIW?page=24&annotation=8NR54SMR)

В Python доступны три логических оператора:

> 	● and — логическое умножение «И»;  
> 	● or — логическое сложение «ИЛИ»;  
> 	● not — логическое отрицание «НЕ».

Логика их работы

![[_attachments/Установка и настройка Python/image-24-x56-y63.png]]
![[_attachments/Установка и настройка Python/image-25-x56-y782.png]]

Вычислим високосный год в Григорианском  календаре поэтапно:

```python
year = int(input("Введите год в формате yyyy: "))  
if year % 4 != 0:  
	print("Обычный")  
elif year % 100 == 0:  
	if year % 400 == 0:  
		print("Високосный")  
	else:  
		print("Обычный")  
else:  
	print("Високосный")
```

А теперь выберем все случаи, когда год обычный и запишем их в одну строку:

```python
if year % 4 != 0 or year % 100 == 0 and year % 400 != 0:
	print("Обычный")  
else:  
	print("Високосный")
```


# Ленивый if

Если в  логическом выражении есть оператор or и первое значение то есть левое вернуло  истину, дальнейшая проверка не происходит, возвращается True. 
Если в  логическом выражении есть оператор and и левая половина вернула ложь, то  возвращается False без проверки правой половины выражения.

# Проверка на вхождение, in

Оператор `in` проверяет вхождение элемента в последовательность.

```python
data = [0, 1, 1, 2, 3, 5, 8, 13, 21]  
num = int(input("Введи число: "))  
if num in data:  
	print("Леонардо передаёт привет!")
```

```python
data = [0, 1, 1, 2, 3, 5, 8, 13, 21]  
num = int(input("Введи число: "))  
if num not in data:  
	print("Леонардо грустит :-(")
```

# Тернарный оператор

позволяет записать 4  логические строки в одну.

Было:

```python
my_math = int(input("2 + 2 = "))  
if 2 + 2 == my_math:  
	print("Верно!")  
else:  
	print("Вы уверены?")
```

Стало:

```python
my_math = int(input("2 + 2 = "))  
print("Верно!" if 2 + 2 == my_math else "Вы уверены?")
```

Слева от `if` записываем выражение для истины, справа от `else` результат для лжи.

***❗❗❗ PEP-8! требует, чтобы длина строки кода не превышала 80 символов.  Современные мониторы с широкими экранами и высоким разрешением с  легкостью вмещают больше символов. Поэтому ограничение было  пересмотрено и увеличено до 120 символов. 
Более длинные строки кода в  Python недопустимы, т.к. снижают его читаемость. ***
