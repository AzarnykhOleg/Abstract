---
date: 06.05.2026
tags: network
authors: AzOV
Abstract:
---
Nmap (Network Mapper) — мощный инструмент с открытым исходным кодом для исследования сети и аудита безопасности. Использует «сырые» IP-пакеты для определения доступных хостов, открытых портов, версий служб, типов ОС и сетевых фильтров. Поддерживает множество техник сканирования, от простого ping до сложного обхода IDS

<a href="https://nmap.org/man/ru">nmap.org</a>

.

## Установка

**Linux (Debian/Ubuntu):**

```bash
sudo apt-get install nmap
```

**Windows:**Скачать установщик с

<a href="https://nmap.org/download.html">nmap.org/download.html</a>

. При установке добавляется в PATH.

**macOS:**

```bash
brew install nmap
```

Проверка версии: `nmap --version`

## Базовый синтаксис

```
nmap [тип сканирования] [опции] {цель}
```

**Цели можно задавать как:**- Один IP: `192.168.1.1`- Диапазон: `192.168.1.1-100`- CIDR: `192.168.1.0/24`- Домен: `scanme.nmap.org`- Список из файла: `-iL targets.txt`

## Типы сканирования

> TCP SYN (`-sS`) — полуоткрытое сканирование (по умолчанию для root). Быстрое и незаметное, так как не завершает handshake.  
> TCP Connect (`-sT`) — полное трёхстороннее рукопожатие (используется если нет прав root).  
> UDP (`-sU`) — сканирование UDP-портов (медленное, но необходимо для DNS, SNMP и т.д.).  
> TCP FIN/NULL/Xmas (`-sF`, `-sN`, `-sX`) — обход простых firewall (отправка пакетов с нестандартными флагами)
> 
> <a href="https://koobik.net/cheatsheet/nmap">koobik.net</a>
> 
> .

|Флаг|Тип|Описание|
|---|---|---|
|`-sS`|TCP SYN|Полуоткрытое сканирование (stealth)|
|`-sT`|TCP Connect|Полное соединение через системный вызов connect()|
|`-sU`|UDP|Сканирование UDP-портов|
|`-sF`|FIN|FIN-сканирование (обход firewall)|
|`-sX`|Xmas|Все флаги TCP (FIN+PSH+URG)|
|`-sN`|NULL|Пакеты без флагов|
|`-sA`|ACK|ACK-сканирование (определение фильтров)|
|`-sV`|Version|Определение версий служб|
|`-sP`|Ping|Только обнаружение хостов|
|`-sO`|IP Protocol|Сканирование IP-протоколов|

## Обнаружение хостов (Host Discovery)

> По умолчанию Nmap выполняет ping-сканирование перед портами. Чтобы пропустить этот этап и сканировать все указанные хосты, используйте `-Pn` (предполагает, что хост жив)
> 
> <a href="https://szybnev.cc/a-complete-guide-to-nmap">szybnev.cc</a>
> 
> .

|Опция|Описание|
|---|---|
|`-sL`|Только составить список (разрешение DNS)|
|`-sP` или `-sn`|Ping-сканирование (только обнаружить живые хосты)|
|`-Pn`|Не пинговать, считать все хосты активными|
|`-PS[port]`|TCP SYN ping (порт 80 по умолчанию)|
|`-PA[port]`|TCP ACK ping|
|`-PU[port]`|UDP ping|
|`-PE`|ICMP echo request (классический ping)|
|`-PR`|ARP-сканирование (для локальной сети)|
|`-n`|Не выполнять DNS-разрешение (ускоряет сканирование)|

**Примеры:**

```bash
# Найти живые хосты в сети
nmap -sn 192.168.1.0/24
# ARP-сканирование локальной сети (быстрое)
nmap -PR -sn 192.168.1.0/24
```

## Определение портов

> По умолчанию Nmap сканирует 1000 самых популярных портов. Для полного сканирования используйте `-p-` (все 65535) или конкретные диапазоны
> 
> <a href="https://koobik.net/cheatsheet/nmap">koobik.net</a>
> 
> .

|Опция|Описание|
|---|---|
|`-p 22`|Конкретный порт|
|`-p 1-1000`|Диапазон|
|`-p 22,80,443`|Несколько портов|
|`-p-`|Все 65535 портов|
|`-p http,https`|По имени службы|
|`-F`|Быстрое сканирование (100 портов)|
|`--top-ports 1000`|N самых популярных портов|
|`-r`|Сканировать последовательно (не случайно)|

## Определение служб и версий

> Флаг `-sV` опрашивает открытые порты для определения точной версии службы (например, Apache 2.4.41). Интенсивность можно регулировать
> 
> <a href="https://nmap.org/man/ru/man-version-detection.html">nmap.org</a>
> 
> .

```bash
nmap -sV 192.168.1.1
# Углубленное определение версий
nmap -sV --version-all 192.168.1.1
```

## Определение операционной системы

> `-O` активирует технику fingerprinting — сравнение ответов хоста с базой данных сигнатур ОС. Требует хотя бы один открытый и один закрытый порт для точного определения
> 
> <a href="https://szybnev.cc/a-complete-guide-to-nmap">szybnev.cc</a>
> 
> .

```bash
nmap -O 192.168.1.1
# Агрессивное сканирование (ОС + версии + скрипты + трассировка)
nmap -A 192.168.1.1
```

## NSE (Nmap Scripting Engine)

> Мощный движок сценариев на языке Lua для автоматизации задач: поиск уязвимостей, брутфорс, сбор информации. Скрипты находятся в `/usr/share/nmap/scripts/`
> 
> <a href="https://nmap.org/man/ru/man-nse.html">nmap.org</a>
> 
> .

**Категории скриптов:**- `default` — стандартные скрипты безопасности  
- `safe` — не влияют на цель  
- `intrusive` — агрессивные (могут вызвать сбои)  
- `vuln` — поиск уязвимостей  
- `exploit` — эксплуатация уязвимостей

```bash
# Запуск стандартных скриптов
nmap -sC 192.168.1.1
# Или явно
nmap --script default 192.168.1.1
# Конкретный скрипт
nmap --script http-title 192.168.1.1
# Скрипт с аргументами
nmap --script ftp-brute --script-args userdb=users.txt,passdb=pass.txt 192.168.1.1
# Поиск уязвимостей
nmap --script vuln 192.168.1.1
```

## Обход Firewall и IDS

> Методы для скрытия сканирования и обхода простых фильтров
> 
> <a href="https://szybnev.cc/a-complete-guide-to-nmap">szybnev.cc</a>
> 
> .

|Опция|Описание|
|---|---|
|`-f`|Фрагментация пакетов (разбивает заголовки)|
|`--mtu 16`|Задать конкретный размер фрагмента|
|`-D RND:10,192.168.1.2`|Маскировка под декои (ложные источники)|
|`-S 192.168.1.100`|Подмена source IP (требуется root)|
|`-e eth0`|Использовать конкретный интерфейс|
|`-g 53`|Указать source port (например, 53 для видимости как DNS)|
|`--source-port 53`|То же самое|
|`--data-length 200`|Добавить случайные данные к пакетам|
|`--randomize-hosts`|Сканировать хосты в случайном порядке|
|`--ttl 5`|Установить маленькое TTL|
|`--spoof-mac Apple`|Подмена MAC-адреса|
|`--badsum`|Отправить пакеты с неверной контрольной суммой (тестирование firewall)|

**Пример обхода:**

```bash
nmap -f -T0 -n -Pn --data-length 200 -D RND:5 192.168.1.1
```

## Управление временем и производительностью

> Nmap предлагает шаблоны скорости от T0 (медленно, скрытно) до T5 (агрессивно, быстро)
> 
> <a href="https://koobik.net/cheatsheet/nmap">koobik.net</a>
> 
> .

|Шаблон|Описание|
|---|---|
|`-T0`|Параноик (обход IDS)|
|`-T1`|Незаметный|
|`-T2`|Вежливый (медленно)|
|`-T3`|Нормальный (по умолчанию)|
|`-T4`|Агрессивный (быстрые сети)|
|`-T5`|Безумный (очень быстро, может терять данные)|

**Тонкая настройка:**

```bash
--min-rate 1000        # Минимум 1000 пакетов в секунду
--max-rate 5000        # Максимум 5000 пакетов в секунду
--max-retries 3        # Максимум попыток на порт
--host-timeout 30m     # Пропускать хост, если сканирование > 30 минут
--scan-delay 5s        # Задержка между портами
```

## Форматы вывода

|Опция|Описание|
|---|---|
|`-oN file.txt`|Обычный текст (readable)|
|`-oX file.xml`|XML (для программной обработки)|
|`-oG file.gnmap`|Grepable (для grep/awk)|
|`-oS file.txt`|Script Kiddie (1337 speak)|
|`-oA basename`|Сразу все три формата (basename.nmap, basename.xml, basename.gnmap)|
|`-v`|Подробный вывод (дважды -vv для максимума)|
|`-d`|Отладка (уровни 1-9)|
|`--open`|Показывать только открытые порты|
|`--reason`|Показывать причину определения состояния порта|
|`--packet-trace`|Показывать все отправленные/принятые пакеты|

## Практические примеры

**Быстрое обнаружение веб-серверов в сети:**

```bash
nmap -p80,443,8080,8443 -sV -oG - 192.168.1.0/24 | grep open
```

**Скрытое сканирование Top 1000 портов с обходом firewall:**

```bash
nmap -sS -T2 -f -n -Pn --data-length 200 --randomize-hosts --spoof-mac Cisco 192.168.1.1
```

**Полное сканирование (aggressive) для CTF:**

```bash
nmap -sV -sC -O -p- -T5 --min-rate 3000 -oA full_scan 10.10.10.10
```

**Сканирование IPv6:**

```bash
nmap -6 fe80::1
```

**Список целей из файла с исключениями:**

```bash
nmap -iL targets.txt --exclude 192.168.1.1,192.168.1.254
```

---

# Ссылки

<a href="https://nmap.org/man/ru">nmap.org</a>

— официальная документация

<a href="https://koobik.net/cheatsheet/nmap">koobik.net</a>

— шпаргалка по Nmap

<a href="https://szybnev.cc/a-complete-guide-to-nmap">szybnev.cc</a>

— полное руководство